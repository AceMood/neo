/* @flow */

import {
  isObject,
  isArray,
  typeOf
} from './types';

/**
 * deeply merge two objects
 * @param dest   destination object
 * @param source source object
 */
export default function merge(dest: Object, source: Object) {

};